powershell.exe -Command "& { param([string]$text='ya ebal'); $TTS=New-Object -ComObject SAPI.SpVoice; foreach ($voice in $TTS.GetVoices()) { if ($voice.GetDescription() -like '*English*') { $TTS.Voice = $voice } }; $TTS.Speak($text) }"
